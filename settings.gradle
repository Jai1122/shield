plugins {
    // Lets Gradle auto-download a matching JDK toolchain when one isn't installed.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'aireview'
